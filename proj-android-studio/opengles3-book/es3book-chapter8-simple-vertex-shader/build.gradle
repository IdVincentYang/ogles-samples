apply from: "$project.rootDir/opengles3-book.gradle"

android {
    defaultConfig {
        externalNativeBuild {
            cmake {
                targets 'Simple_VertexShader'
            }
        }
    }
}
